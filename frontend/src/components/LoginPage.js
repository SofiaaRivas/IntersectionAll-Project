import React from "react";
import Login from "../LoginFormComponent.js"; // login form

const LoginPage = () => {
  return (
    <div style={{ display: "flex", justifyContent: "center", alignItems: "center", height: "100vh" }}>
      <Login />
    </div>
  );
};

export default LoginPage;
